# =========================================================================
# Configurações de Ambiente da API Central (Delphi + Horse)
# Copie este arquivo para um novo arquivo chamado `.env` no mesmo diretório.
# =========================================================================

# Porta em que a API Central estará disponível externamente e escutará internamente
PORT=3333

# Chave Mestra secreta usada para assinar e validar os tokens JWT
SECRET_KEY=Portal@3694_05557971000150

# Senha padrão do usuário SYSDBA no Firebird (padrão: masterkey)
DB_PASSWORD=masterkey
